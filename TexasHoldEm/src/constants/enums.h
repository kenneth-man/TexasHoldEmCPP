#pragma once

namespace Enums {
	enum GameState {
		AUTH,
		LOGIN,
		REGISTER,
		TITLE,
		NEWGAME,
		CONTINUE,
		INSTRUCTIONS,
		LEADERBOARD,
		OPTIONS,
		QUIT,
		QUIT_0,
		QUIT_1
	};
	enum Crypt {
		ENCRYPT,
		DECRYPT
	};
}